import TYPES from '../types';

export const normalAction = (mode) => ({
  type: TYPES.NORMAL_COUNT,
  payload: mode,
});

export const normalAction2 = (mode) => ({
  type: TYPES.NORMAL_COUNT,
  payload: mode,
});
